#include <stdio.h>
#include <string.h>

int main() {
   char str[20];
   int i;
   printf("Enter string : ");
   scanf("%s",str);
   for(i=0;i<=strlen(str);i++)
   {
       if(str[i]>=97  && str[i]<=122)
       str[i]= str[i]-32;
   }
   printf("uppercase string :%s",str);
    return 0;
}
